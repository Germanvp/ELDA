int a = 0;

int func(int i, int j, int k) {
    int b = 2;
    a = i + j * k;
    out("B");
    out(b);
    out("A");
    out(a);
    return a+b;
}

void winter(string p){
    p = "Winter is Here";
    out(p);
}

void main() {
    int p = func(3+5, a, 5);
    winter("Heho");
    func(3+4, a + 1+ 5, 5);
    a = 2;
}